;; -*- lexical-binding: t; -*-

;; Evil collection
(use-package evil-collection
  :after evil
  :config
  ;;(evil-collection-init '(elfeed mu4e custom magit ibuffer help helpful term ansi-term)))
  (evil-collection-init))

(provide 'init-evil-collection.el)
