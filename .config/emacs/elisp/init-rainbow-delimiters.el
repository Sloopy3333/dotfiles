;; -*- lexical-binding: t; -*-

;; Rainbow-delimiters
;; highlights matching brackets in same color
(use-package rainbow-delimiters
  :hook (prog-mode . rainbow-delimiters-mode))

(provide 'init-rainbow-delimiters.el)
